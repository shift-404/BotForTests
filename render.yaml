services:
  - type: web
    name: your-bot-name
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 & python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
